# CLAUDE.md — памятка по проекту «Бани Вятки»

Лендинг компании «Бани Вятки» (Киров): мобильная баня «Подкова» — готовая
конструкция, которую привозят и ставят на участке. Покупатель делегирует:
страница говорит на языке «что вы не будете делать», а не «какая у нас баня».
Каждый блок заканчивается числом или проверяемым фактом.

Репозиторий публичный: https://github.com/volkov43-beep/banivyatki-landing
Сайт: https://volkov43-beep.github.io/banivyatki-landing/ (позже — banivyatki.ru).

## Правила безопасности (читать первыми)

- Репозиторий публичный: никаких ключей, вебхуков, токенов и паролей в коде
  и в истории коммитов. Ключи только через переменные окружения (`.env`,
  в `.gitignore`) и секреты GitHub Actions: ключ Яндекс Карт — только через
  секрет `VITE_YMAPS_KEY`, его значение не печатать ни в чат, ни в консоль,
  ни в логи сборки. Проверять `git log -p -S` перед пушем.
- Никаких персональных данных клиентов: имён, телефонов, адресов, ссылок на
  их страницы. В данные сайта попадают только населённые пункты
  (`src/data/objects.js`). Отзывы — только с именем как на площадке и датой.
- Никаких себестоимостей, маржи и внутренних прайсов. На сайте только цены
  «от» из `src/data/calculator.js`.
- Реквизиты компании (банковские) на сайте не публикуем без отдельного
  решения. В политике конфиденциальности — только ИНН/ОГРН/адрес.
- Ссылку вебхука Битрикс24 не запрашивать и не принимать: она живёт только
  на сервере (Beget), в лендинге — только адрес PHP-обработчика.
- Формы пока никуда не отправляют заявки. Рекламу (Директ) не запускать до
  подключения Битрикс24.

## Стек и сборка

- Vite 7 + React 19 + Tailwind CSS 3. JavaScript, без TypeScript.
- `npm install`, `npm run dev` (локально), `npm run build` (в `dist/`),
  `npm run preview` (просмотр сборки).
- Две страницы Vite: `index.html` (лендинг) и `privacy/index.html` (`/privacy/`).
  Входы заданы в `vite.config.js`.
- Пути к файлам из `public/` в коде — через `import.meta.env.BASE_URL`,
  в HTML — через `%BASE_URL%`. Иначе на GitHub Pages ссылки сломаются.
- Переменные окружения: `VITE_YMAPS_KEY` — ключ Яндекс Карт (см. `.env.example`).

### Деплой на GitHub Pages

Workflow `.github/workflows/deploy-pages.yml`: при пуше в `main` (или вручную
через «Run workflow») собирает сайт с `--base=/banivyatki-landing/` и публикует
`dist/` через `actions/deploy-pages`. Ключ карт передаётся в сборку из секрета
репозитория `VITE_YMAPS_KEY` (Settings → Secrets and variables → Actions).
В Settings → Pages источник должен быть «GitHub Actions».

Рабочий процесс: ветка от `main`, коммиты, push, pull request. После мержа
ветку пересоздавать от `main`, а не стекать поверх влитой истории.

## Структура

```
index.html                    — лендинг: preload картинки первого экрана (одна ссылка, подбирается
                                скриптом под ширину экрана — иначе Firefox ругается на media), favicon
privacy/index.html            — страница политики
.env.example                  — VITE_YMAPS_KEY= (сам .env в репозиторий не попадает)
scripts/geocode.mjs           — координаты мест через HTTP Геокодер Яндекса → objects.js
src/
  main.jsx                    — вход лендинга: captureUtm(), trackPhoneClicks()
  privacy.jsx                 — вход страницы политики
  App.jsx                     — порядок блоков
  index.css                   — Tailwind, базовые стили, фокус, reduced-motion,
                                .dg-label для подписей схем, .bv-map-point для карты
  styles/tokens.css           — дизайн-токены (палитра, типографика, сетка)
  data/calculator.js          — цены и тексты калькулятора (единственное место)
  data/benefits.js            — «Что вы получаете»: четыре плитки, кнопка
  data/reviews.js             — отзывы: площадки, семь карточек, отзыв для бизнеса
  data/faq.js                 — частые вопросы: 18 вопросов, plainAnswer() для JSON-LD
  data/objects.js             — где стоят бани: только названия мест и координаты
  data/works.js               — «Наши работы»: шесть объектов, подписи, фото, сезон и карточка для калькулятора
  data/visit.js               — «Посмотрите баню до покупки»: тексты карточек, ленты, типы записи
  lib/
    submitLead.js             — единственная точка отправки заявок
    track.js                  — цели Яндекс.Метрики, клики по телефону
    utm.js                    — UTM в sessionStorage
    visit.js                  — событие «записаться на показ» (ссылка из FAQ → форма блока показа)
    calc.js                   — события «рассчитать такую» («Наши работы» → калькулятор)
                                и «открыть калькулятор» без выбора (кнопка «Что вы получаете»)
    callback.js               — обещание ответить/перезвонить по московскому времени
    phone.js                  — маска и нормализация телефона
    ymaps.js                  — загрузка JavaScript API Яндекс Карт v3 по требованию
  components/
    Header.jsx                — шапка поверх первого экрана: логотип, телефон
    Button.jsx                — единый компонент кнопки: primary / secondary / link, размеры lg / md, стрелка arrow
    SectionHero.jsx           — первый экран
    SectionBenefits.jsx       — «Что вы получаете» (#benefits): четыре плитки с иконками, кнопка в калькулятор
    benefits/BenefitIcons.jsx — четыре иконки инлайновым SVG (линии 1,5 px, 40 × 40)
    SectionComparison.jsx     — «Чем Подкова отличается от бани-бочки»
    CrossSectionDiagram.jsx   — главная схема: чертёж diagram-sravnenie.webp + размеры в SVG
    AdvantageItem.jsx, PhotoSlot.jsx
    SectionCalculator.jsx     — калькулятор
    calculator/               — Segmented, FloorPlan, CalculatorCard, LeadForm
    SectionInside.jsx         — «Что внутри»: три раздела, фото тем и карточки «Мелочей»
    ResponsivePhoto.jsx       — фото в двух ширинах (srcset 1280/640, ratio 4/3 или 3/4, widths), photoSrc()
    AnnotatedPhoto.jsx        — фото с пометкой: один SVG 1280 × 960 (<image> + линии и текст поверх),
                                файл 640/1280 выбирается по ширине × плотности (у SVG <image> нет srcset)
    SectionWorks.jsx          — «Наши работы» (#works): карточки объектов + карта
    works/                    — WorkCard (карточка), Lightbox (увеличение), MapBlock (карта Яндекса, список мест)
    SectionProcess.jsx        — «Как проходит заказ»: пять шагов, «Делаем мы / Делаете вы»
    SectionReviews.jsx        — «Отзывы»: каскад в две колонки, «Показать ещё» на телефоне
    ReviewCard.jsx            — карточка отзыва (блок «Отзывы» и вкладка «Для бизнеса»)
    SourceIcon.jsx            — иконка площадки: круглая для карточек, квадратная для плашек
    RatingStars.jsx           — звёзды рейтинга инлайн-SVG, заполнение из числа
    SectionShowroom.jsx       — «Посмотрите баню до покупки» (#showroom): три карточки и форма записи
    showroom/                 — VisitCard (карточка с лентой), VisitForm (форма записи)
    SectionFaq.jsx            — «Частые вопросы»: аккордеон на <details>, JSON-LD FAQPage
    FaqQuestionForm.jsx       — карточка «Не нашли ответ?» с формой вопроса на surface
    SectionFinal.jsx          — финальная форма (#final) на forest: заголовок, обещание, FinalForm
    FinalForm.jsx             — форма «Получить расчёт»: имя, телефон, «Звонок / MAX», form: "final"
    Footer.jsx                — подвал на ink: логотип, телефон, часы, адрес, ВКонтакте, копирайт, политика
    diagrams/                 — Diagram.jsx (общий каркас, --s), MicroDiagram и схемы,
                                LabeledDrawing.jsx — готовый чертёж WebP + подписи в одном SVG
  pages/PrivacyPage.jsx       — политика конфиденциальности
public/photos/                — WebP-фото, логотип, чертежи diagram-*.webp,
                                иконки площадок review-*.webp, rating-*.webp,
                                фото блока показа shourum-*, proizvodstvo-*, video-zvonok (1280 и 640),
                                фото объектов obekt-* (1280 и 640),
                                фото блока «Что внутри» vnutri-* (1280 и 640; vnutri-uteplenie только 640)
public/favicon-32.png, apple-touch-icon.png
```

## Дизайн-система

Токены объявлены в `src/styles/tokens.css` и подключены в Tailwind через
`theme.extend` (`tailwind.config.js`). Использовать классы Tailwind
(`bg-ink`, `text-surface`, `text-heading` …), не хардкодить значения.
Дизайн-токены, гарнитуру и сетку не менять без отдельного задания.

### Палитра

| Токен | Значение | Где |
| --- | --- | --- |
| `ink` | #1A1512 | тёмный фон (hero, сравнение), крупный текст на светлом |
| `forest` | #10231D | тёмно-зелёный из логотипа: только фон тёмных карточек и блоков |
| `surface` | #F4EADF | светлый фон секций, текст на тёмном |
| `surface-2` | #ECE1D3 | панель на фоне surface (форма калькулятора, карточки отзывов, плашки) |
| `accent` | #C98A2E | акценты, размерные линии, кнопки, цена, точки на карте |
| `accent-hover` | #B57C29 | accent темнее на 10 %: наведение на главную кнопку |
| `accent-active` | #A57023 | accent темнее на 18 %: нажатие на кнопку |
| `muted` | #6E7A6A | границы и второстепенный текст на светлом (3,8:1 — AA для мелкого текста не проходит, известно) |
| `alert` | #8C3F1D | проблемные места на схемах, ошибки на светлом |
| `muted-on-dark` | #9AA396 | второстепенный текст и линии на фоне ink / forest (6,9:1) |
| `alert-on-dark` | #D9663A | проблемные места на схемах и ошибки на тёмном фоне (5,1:1) |

На тёмном фоне использовать только `*-on-dark`. Не использовать: зелёный
как доминанту, терракоту около #D97757, градиентные заливки, тени под карточками.

Правила для `forest`:
- Только **фон** тёмных карточек и блоков. Не для текста и не для заголовков
  на бежевом: там он почти не отличается от чёрного.
- На `forest` те же цвета текста, что на `ink`: светлый — `surface`,
  второстепенный — `muted-on-dark`, акцент и кнопки — `accent`. Контраст
  проверен: 13,8 / 6,3 / 5,6.
- Блоки на `forest`: «Что вы получаете», «Как проходит заказ», финальная
  форма; карточки на `forest`: форма записи на показ (`showroom/VisitForm`).
  Карточка вопроса FAQ — на `surface`, не на `forest`. Подвал — на `ink`,
  чтобы отделяться от зелёной формы. Hero, сравнение и карта — на `ink`.
- Поля на `forest`: прозрачный фон, рамка `rgba(244,234,223,0.28)`, текст
  `surface`, плейсхолдер `muted-on-dark`; `Segmented` со `scheme="dark"`.

### Кнопки

Все кнопки страницы — через `components/Button.jsx`. Три типа (`variant`):

| Тип | Вид | Где |
| --- | --- | --- |
| `primary` | заливка `accent`, текст `ink`, скругление 4 px | одна на блок: отправка формы, переход в калькулятор, запись на показ |
| `secondary` | прозрачный фон, рамка 1,5 px; текст `accent` на светлом (`scheme="light"`), `surface` на тёмном и зелёном (`scheme="dark"`) | второе действие рядом с главной («Показать ещё отзывы») |
| `link` | текст `accent`, подчёркивание полупрозрачное `rgba(201,138,46,0.4)` | мелкие действия внутри карточек: «Рассчитать такую», «Читать полностью», «изменить», места в строке «А ещё» |

Состояния: наведение — `primary` фон `accent-hover` и мягкая тень
`0 4px 14px rgba(26,21,18,0.22)`, `secondary` заливается (`accent` на светлом,
`surface` на тёмном, текст `ink`), `link` — подчёркивание непрозрачное;
нажатие — `accent-active` / `surface-2`, без сдвига; фокус с клавиатуры —
рамка 2 px `accent` с отступом 3 px (общее `:focus-visible` в `index.css`).
Переходы 150 мс только по цвету и тени, ничего не двигается и не растёт.

Стрелка (`arrow`): у главных кнопок, ведущих к другому блоку (калькулятор,
форма записи, блок показа), в конце текста тонкий шеврон, нарисованный в коде
по правилам иконок: линия 1,5 px, скруглённые концы, цвет от текста кнопки
(`currentColor`), 20 × 20. При наведении сдвигается вправо на 3 px за 150 мс
(`group-hover`), при `prefers-reduced-motion` не двигается. У кнопок отправки
форм («Получить расчёт», «Задать вопрос», «Записаться») и у полоски на
телефоне стрелки нет.

«Дыхание» — только у одной кнопки на странице, «Рассчитать стоимость» на
первом экране (`SectionHero`): когда кнопка впервые попадает в поле зрения
(IntersectionObserver), один цикл 1,2 с — тень `accent` нарастает и уходит
(`@keyframes bv-breathe` в `index.css`, класс `.bv-breathe`), размер и
положение не меняются. Повторно не запускается; если до появления навели
мышь, нажали или дали фокус — не запускается вовсе; при
`prefers-reduced-motion` не выполняется (проверка и в JS, и в CSS). Других
анимаций появления на странице нет.

Кнопка внизу блока («Собрать свою баню», «Узнать цену Подковы», «Посмотреть
баню вживую», «Рассчитать стоимость» в «Как проходит заказ») — по центру,
подводка над ней тоже по центру. Кнопки в карточках и формах — как в них
принято.

Размеры (`size`): `lg` — крупная, формы и кнопки блоков: min-height 56 px,
текст 17 px, `px-8`; `md` — обычная, внутри карточек и полоска на телефоне:
44 px, 15 px. У `link` размер задаёт кегль: `lg` 17 px жирный, `md` 15 px
жирный, `sm` 14 px обычный. `as="a"` для ссылок, `full` — на всю ширину,
`fullMobile` — на всю ширину до 1023 px. Тексты кнопок и высоту одного
размера не разводить между блоками. Не через `Button`: переключатели
`Segmented` (радиогруппы), карточки калькулятора (радио с подписью
«Выбрать»), «×» и стрелки увеличения фото, аккордеон FAQ, телефон в шапке.

### Иконки

На странице иконки только в блоке «Что вы получаете»
(`benefits/BenefitIcons.jsx`): рисуются в коде инлайновым SVG, готовые наборы
и внешние библиотеки не используются. Только линии без заливок и градиентов,
толщина 1,5 px, скруглённые концы и стыки, цвет `accent` через
`currentColor`, 40 × 40 в viewBox `0 0 40 40`, одинаковая оптическая
плотность у всех, простая узнаваемая геометрия, без теней и текста внутри,
`aria-hidden="true"` (смысл несёт заголовок плитки).

### Типографика

Гарнитура Onest (Google Fonts), веса 400 и 700, кириллица.

| Класс | Размер | Вес | Интерлиньяж |
| --- | --- | --- | --- |
| `text-heading` | clamp(32px, 5vw, 56px) | 700 | 1.05 |
| `text-lead` | clamp(18px, 2.2vw, 24px) | 400 | 1.45 |
| `text-title` | 20px | 700 | 1.2 |
| `text-body` | 17px | 400 | 1.5 |
| `text-label` | 14px | 400 | 1.2 |

Длина строки не больше 70 символов (`max-w-measure`). Заголовок первого
экрана — своя формула `clamp(40px, 3.38vw, 49px)`, чтобы укладываться в три строки.
Заголовок и подзаголовок каждого блока — по центру на всех ширинах;
заголовки внутри карточек, подписи к фото и текст карточек — по левому краю.
Запрещено: разрядка и капс, выделение слова в заголовке цветом или курсивом,
стрелки в тексте кнопок, моноширинный шрифт для подписей. Исключение по
заданию — капсулы-разделители разделов в «Что внутри» (капс, разрядка 0,08em).

### Сетка и брейкпоинт

- Контейнер `max-w-container` (1200 px), отступы `px-gutter` 24 / `md:px-gutter-lg` 48.
- Секции `py-section-y` 64 / `md:py-section-y-lg` 96.
- Брейкпоинт раскладки — **1024 px** (`lg:`): до 1023 мобильная раскладка,
  от 1024 компьютерная. Внутри блоков есть свой порог **600 px**
  (`min-[600px]:`): ниже него пары фото ставятся друг под другом, подписи
  на чертежах заменяются номерами, главная схема разбивается на две.

### Схемы и чертежи

- Микро-схемы — инлайновый SVG в чертёжной манере: контуры без заливки,
  размерные и выносные линии `accent`, подписи через `diagrams/Diagram.jsx`
  (схемы `dark` / `light`, режим `scale`, при котором подписи держат заданный
  размер в px на любом масштабе через CSS-переменную `--s`).
- Готовые чертежи (WebP с прозрачностью) ставятся через **один SVG с общим
  viewBox в размер картинки**: внутри `<image>`, поверх текст и выноски,
  поэтому подписи масштабируются вместе с рисунком. Опорные точки измеряются
  по пикселям файла (альфа-канал, цвет), не на глаз. Текст 14 px через `--s`,
  выноски 1.5 px `accent` с точкой; размерные линии 1 px со стрелками,
  выносные тоньше и полупрозрачные.
- **До 600 px**: у чертежей карточек (`LabeledDrawing`) вместо текста золотые
  номера в кружках и список под картинкой; главная схема сравнения
  (`CrossSectionDiagram`) показывается двумя SVG друг под другом с окнами
  viewBox одинаковой ширины (820), чтобы люди остались одного роста.
- Картинка внутри SVG грузится лениво: `href` ставится по IntersectionObserver
  (у SVG `<image>` нет `loading="lazy"`). У SVG `role="img"` и `aria-label`.

## Порядок блоков на странице

Статус: **готов** / **в работе** / **ждёт данных**. Отправка всех форм —
заглушка (console.log) до подключения Битрикс24, это не статус блока.

1. `Header` — **готов**. Логотип 64/48 px со ссылкой на начало, телефон
   (на телефоне плашка). Клик по телефону — цель `phone_click`.
2. `SectionHero` (`#top`) — **готов**. Картинка `<picture>` (мобильный файл до
   1023, горизонтальный от 1024), заголовок «Баня-Подкова: / шире бочки,
   с ровным полом, под ключ», подзаголовок, цена `от MIN_PRICE`, одна кнопка
   на `#calculator`, факты доверия («1000+ бань с 2012 года», «Гарантия 5 лет»,
   «5,0 на Авито — 40 оценок» — текстом, без ссылки).
3. `SectionBenefits` (`#benefits`) — **готов**. Экран «Что вы получаете» на
   `forest`, подзаголовок «Четыре вещи, ради которых у нас покупают». Четыре
   плитки (`data/benefits.js`): иконка → заголовок `text-title` → текст
   `muted-on-dark`; от 1024 px в ряд, от 600 px 2 × 2, ниже друг под другом;
   без рамок и коробок. Под плитками главная кнопка «Собрать свою баню»:
   цель `benefits_cta`, `requestCalcOpen()` — калькулятор переключается на
   «Себе» и плавно прокручивается к своему началу, сезон и размер не трогаются.
4. `SectionComparison` — **ждёт данных**: вёрстка и тексты готовы, ждём
   чертёж пола и новые иллюстрации в две колонки «в бочке / в Подкове»
   (см. «Что дальше»).
   Тёмный экран `ink`: главная схема бочка/Подкова, подпись, четыре блока
   преимуществ (от 1024 px сетка 2 × 2 по верхнему краю). Порядок в блоке:
   заголовок → схема → текст → фото → подпись. В конце по центру подводка
   «Посчитаем стоимость под ваш участок и комплектацию.» и главная кнопка
   «Узнать цену Подковы» со стрелкой: цель `compare_cta`, `requestCalcOpen()`.
   Главная схема — чертёж `diagram-sravnenie.webp` (1600 × 800, 2:1) с размерами
   поверх: 2000 мм, «высота над головой», «трапик 40 см» (alert-on-dark),
   2400 мм, 2300 мм, «пол 2000 мм» (accent), названия «Баня-бочка» и
   «Баня-Подкова» 18 px жирные surface на одной базовой линии. Опорные точки
   в `G` измерены по пикселям (427 единиц = 2000 мм).
   Фото: парная 1:1 (`shirina-parnaya.webp`), обвязка 4:3 (`obvyazka.webp`),
   проливной пол — пара 4:3 (`prolivnoy-pol-sverhu.webp`, `-razrez.webp`,
   подписи 13 px, сноска про комплектации под парой). «Дуга стен» без фото.
   Фото со скруглением 6 px, без рамок и теней.
   Чертежи `diagram-obvyazka.webp` (1440 × 850) и `diagram-duga.webp`
   (1440 × 1362) через `LabeledDrawing`. В «Двойном проливном поле» пока
   микро-схема `DrainFloorDiagram` (SVG) — её заменит чертёж diagram-pol.png.
5. `SectionInside` — **в работе** (следующее задание; фото утепления есть
   только в 640 px). Светлый экран `surface`. Три раздела, каждый открывает
   разделитель `SectionDivider`: по центру капсула (рамка 1,5 px `accent`,
   пилюля, отступы 7 × 18 px, текст `accent` 13 px 700 капсом, разрядка
   0,08em; до 479 px — 12 px и 0,06em, до 379 px — 11 px, без переноса),
   слева и справа линии 1 px `muted`; отступ над разделителем 112/160 px
   (больше, чем между темами: 80/112). Разделы: «Воздух и тепло» (вентиляция
   «живая баня», «Второе дыхание», утепление парной), «Материалы» (печь,
   дерево по задачам, кровля — три абзаца, Люкс/Комфорт, про базовую
   комплектацию не пишем), «Мелочи, которые замечаешь потом» (две карточки:
   «Фурнитура» и «Слив под рукой», от 768 px в ряд, ниже друг под другом).
   Темы первых двух разделов — строками с чередованием «фото слева / текст
   справа» сквозь оба раздела; схем в блоке нет, только фото
   (`ResponsivePhoto`, вертикальный кадр утепления в 3:4). Фото слива 4:3 —
   `AnnotatedPhoto` с пометкой: рамка 2 px `accent` вокруг крышки люка
   (по швам x 395–890, y 468–570 в файле 1280) и выноска к подписи «крышка
   люка» 14 px на светлой стене над ней. Под фото «Второго дыхания»
   подпись «Продух второго дыхания открывается с полка»; где стоит продух,
   в тексте не пишем. Внизу по центру подводка и кнопка «Посмотреть баню
   вживую» со стрелкой на `#showroom`. Тексты тем — в самом компоненте.
6. `SectionWorks` (`#works`) — **готов** (карта внутри — **ждёт данных**:
   живой вид не проверен, координаты предварительные, см. ниже). Две части одного блока: карточки объектов на `surface-2`, карта
   ниже на `ink` (текст `surface`, второстепенный `muted-on-dark`, плашка
   до загрузки `rgba(244,234,223,0.06)`), стык без зазора и линии. Заголовок
   «Наши работы» и подзаголовок «Подковы, которые уже стоят у наших
   клиентов» по центру на светлой части. Шесть карточек объектов
   (`works/WorkCard`, данные `data/works.js`): фото 4:3 со скруглением,
   подпись буквально из задания (`text-body`, `ink`), текстовая кнопка
   «Рассчитать такую» `accent` (у «Доставки и установки» нет). От 1024 px три
   в ряд, ниже две. Клик по фото — увеличение `works/Lightbox`: два кадра,
   стрелки и клавиши, закрытие по Esc / кнопке / клику вне, свайп на телефоне,
   без таймеров. Кнопка — `requestCalc(season, cardId)` из `lib/calc.js`:
   калькулятор переключается на «Себе», ставит сезон и карточку теми же
   обработчиками и целями, что при ручном выборе, и прокручивает к сетке
   карточек (не к форме): видна цена выбранного размера и соседние варианты.
   Под карточками `works/MapBlock` (бывший `SectionMap`, `#map`): маленький
   заголовок «Где стоят наши бани», прежний подзаголовок, карта Яндекса
   (JavaScript API v3) 480/360 px со скруглением 6 px, строка «А ещё: …»
   с переездами к другим регионам и кнопка «Вся Кировская область», ниже все
   места текстом в 3/2 колонки. Карта: координаты предварительные (скрипт
   геокодера не запускался), живой вид не проверялся.
7. `SectionCalculator` (`#calculator`) — **готов**, отправка — заглушка
   (console.log). Вкладки «Себе / Для бизнеса», сезон, три карточки, панель
   формы, полоска на телефоне; во вкладке «Для бизнеса» отзыв арендодателя.
   Стоит после «Наших работ»: к нему ведут кнопка первого экрана и блока
   «Как проходит заказ» (якорь `#calculator`), «Рассчитать такую» из карточек
   объектов и «Собрать свою баню» (события `lib/calc.js`).
8. `SectionProcess` (`#process`) — **готов**. Экран на `forest`, текст
   `surface`, разделители `muted-on-dark`. Пять нумерованных шагов (заявка,
   просмотр и договор, изготовление, доставка, гарантия) с колонками
   «Делаем мы / Делаете вы». Главная колонка — «Делаете вы» на подложке
   `surface-2` с текстом `ink` и линией `accent` слева, где нечего делать —
   крупное «Ничего». Accent в блоке только у сроков и этой колонки. Под
   шагами по центру итоговая строка и кнопка «Рассчитать стоимость» со
   стрелкой на `#calculator`.
9. `SectionReviews` (`#reviews`) — **готов**. Экран на `surface-2`; плашки
   рейтинга и карточки отзывов на `surface` (`ReviewCard tone="surface"`),
   чтобы не сливаться с фоном. Заголовок «Что говорят владельцы бань», две
   плашки (квадратная иконка площадки, рейтинг со звёздами, число оценок,
   без ссылок), семь карточек. От 1024 px две колонки каскадом
   (отзывы 1 и 2 в первом ряду, дальше в ту колонку, что короче), до 1023 px
   одна колонка: три отзыва и кнопка «Показать ещё отзывы».
10. `SectionShowroom` (`#showroom`) — **готов**. Экран на `surface`
   «Посмотрите баню до покупки», подзаголовок «Три способа — выбирайте
   удобный». Кнопки карточек со стрелкой (ведут к форме записи). Три
   карточки на `surface-2` (`showroom/VisitCard`): «Приезжайте в шоурум»
   (фото слева, адрес Ленина, 71Б акцентом, лента «Что вы увидите» из 4 фото),
   «Приезжайте на производство» (зеркально, адрес Северное Кольцо, лента
   «Как собирают Подкову» из 5 фото с номерами шагов accent; пятое фото —
   тот же файл, что главное фото карточки), «Покажем по видеосвязи»
   (компактная: фото на треть, без ленты). От 1024 фото и текст в две колонки,
   лента в ряд; до 1023 фото → текст → лента в 2 колонки, пятое фото ленты
   на всю ширину. Под карточками одна форма записи `showroom/VisitForm` на
   `forest` (стиль карточки вопроса FAQ): `Segmented` на три части
   «Шоурум / Производство / Видеозвонок» (`size="xs"`), имя, телефон,
   «Звонок / MAX», кнопка «Записаться», согласие. Кнопки карточек прокручивают
   к форме (`scrollIntoView`, без анимации при reduced-motion) и выбирают
   способ. Тексты — `src/data/visit.js`.
11. `SectionFaq` (`#faq`) — **ждёт данных**: вёрстка готова, три ответа ждут
    подтверждения (см. «Известные проблемы»). Экран на `surface-2`. 18 вопросов одним списком,
    аккордеон на `<details>`/`<summary>`, всё свёрнуто, можно открыть
    несколько, без анимации — только поворот «+» в «×». JSON-LD `FAQPage`.
    Под списком карточка `FaqQuestionForm` с формой вопроса: `surface`, рамка
    `rgba(26,21,18,0.12)`, текст `ink`, подзаголовок `muted`, поля на
    `surface-2` с рамкой `muted`, переключатель «Звонок / MAX» со схемой
    `accent`, кнопка `accent`.
12. `SectionFinal` (`#final`) — **готов**. Последний экран перед подвалом на
    `forest` во всю ширину, отступы 80/128 px. Заголовок «Рассчитаем вашу
    баню» и подзаголовок с обещанием (`callbackWhen()`) по центру, форма
    `FinalForm` до 600 px по центру: имя, телефон с маской, «Звонок / MAX»
    (`Segmented` `sm` `full` `dark`), кнопка «Получить расчёт» на всю ширину,
    согласие. После отправки на месте формы «Заявка отправлена. Менеджер
    перезвонит …». Цель `final_submit`.
13. `Footer` — **готов**. `ink`, текст `muted-on-dark`, ссылки `surface`,
    при наведении `accent`. От 1024 px три колонки: логотип 64 px (`logo-128`
    / `logo-256` через srcset, lazy) и рядом в строку «Бани собственного
    производства, с 2012 года» (на телефоне тоже в строку по центру, при
    нехватке места текст под логотипом) / телефон 24 px ссылкой `tel:` и
    «Пн–Пт 9:00–17:00» / «Киров, ул. Ленина, 71Б» и ссылка «ВКонтакте»
    (`https://vk.ru/banivyatki_ru`, новая вкладка, `rel="noopener"`, цель
    `vk_click`). До 1023 px одна колонка по центру в том же порядке. Внизу над
    тонкой линией: «© 2026 Бани Вятки · ООО «Банная философия», ИНН
    9718285920 · Политика конфиденциальности». Без почты, счёта, юридического
    адреса, адреса производства, карты, кнопок и форм. Стык с формой без
    зазора и без линии.

Все блоки страницы собраны. Порядок в `App.jsx` совпадает со списком выше.

### Фоны блоков

Правило: **два одинаковых фона подряд не ставим** — соседние блоки всегда
разного цвета, стыки без линий и зазоров. Менять фон одного блока — значит
проверить оба соседних.

| № | Блок | Фон | Текст / второстепенный |
| --- | --- | --- | --- |
| 1–2 | Шапка и первый экран | фото на `ink` | `surface` / `muted-on-dark` |
| 3 | Что вы получаете | `forest` | `surface` / `muted-on-dark` |
| 4 | Сравнение с бочкой | `ink` | `surface` / `muted-on-dark` |
| 5 | Что внутри | `surface` | `ink` / `muted` |
| 6а | Наши работы — карточки | `surface-2` | `ink` / `muted` |
| 6б | Наши работы — карта | `ink` | `surface` / `muted-on-dark` |
| 7 | Калькулятор | `surface` (панель формы `surface-2`) | `ink` / `muted` |
| 8 | Как проходит заказ | `forest` (колонка «Делаете вы» `surface-2`) | `surface` / `muted-on-dark` |
| 9 | Отзывы | `surface-2` (плашки и карточки `surface`) | `ink` / `muted` |
| 10 | Посмотрите баню до покупки | `surface` (карточки `surface-2`, форма `forest`) | `ink` / `muted` |
| 11 | Частые вопросы | `surface-2` (карточка вопроса `surface`) | `ink` / `muted` |
| 12 | Финальная форма | `forest` | `surface` / `muted-on-dark` |
| 13 | Подвал | `ink` | `surface` / `muted-on-dark` |

## Решения по тексту

- «Подкова» всегда с большой буквы, когда это название продукта
  («Баня-Подкова», «У Подковы пол прямой»). Строчная — только для формы
  («формы подкова»). В подписях главной схемы — «Баня-бочка» и «Баня-Подкова».
- Наценку за безнал и за оплату картой на сайте не упоминаем.
- Телефон в формах не заменяет заявку: везде ведём на форму (калькулятор,
  форма вопроса в FAQ), чтобы отслеживать конверсию. Под FAQ телефона нет.
- В карточках только «от», без диапазонов «от–до». Минимальная цена
  калькулятора = цена на первом экране: **293 000 ₽**, первый экран берёт
  `MIN_PRICE` из `src/data/calculator.js`, руками не писать.
- В готовых банях 4–6 м (тёплый сезон) не упоминать двойной проливной пол
  и вентиляцию «живая баня» — их там нет.
- Не упоминать басту, окна 300×300, мощность подключения (киловатты и сечения).
- Сумму задатка в блоке «Как проходит заказ» не пишем; в FAQ — «от 5 000 ₽»
  (подтверждено). Про очередь и сезонность не пишем.
- Пол Подковы 2000 мм, наружная ширина 2400, высота 2300 — цифры публичные.
- Канон цифр: бани с 2012 года, Подкова с 2019-го, больше 1000 бань, из них
  больше 300 Подков; гарантия 5 лет, продлевается ежегодным обслуживанием.
- Без анимаций появления по скроллу, таймеров, всплывающих окон, бейджей
  «Акция»/«Хит», карусели в отзывах. Без иконок в карточках блока сравнения
  и калькулятора; исключение — плитки «Что вы получаете», там иконки по
  правилам раздела «Иконки». Одна кнопка на первом экране.
- Ссылок на avito.ru и yandex.ru на странице нет: площадки показаны иконками
  и текстом. Отзывы — настоящие, тексты не переписывать (только сокращать
  через «…» и править явные опечатки).

## Формы и заявки

- `submitLead(payload)` в `src/lib/submitLead.js` — единственная точка
  отправки. **Сейчас пишет в console.log и возвращает `{ ok: true }`.**
- Payload: `form` (`calculator` / `business` / `faq_question` / `visit` / `final`),
  `season`, `card_id`, `card_title`, `size`, `price_shown` (число, обязательно
  для калькулятора), `phone` (+7XXXXXXXXXX), `name`, `company` и `comment`
  (только бизнес), `question` (только faq_question), `visit_type`
  (`showroom` / `production` / `video`, только visit), `contact_method`
  (`call` / `max`), `utm_source/medium/campaign/content/term`, `page_url`,
  `submitted_at` (ISO).
- UTM (`src/lib/utm.js`): при первом заходе сохраняются в `sessionStorage`
  (`bv_utm`), при отправке берутся оттуда.
- Защита от спама в `LeadForm`, `FaqQuestionForm`, `VisitForm` и `FinalForm`: скрытое поле-ловушка
  (honeypot `website`) и проверка, что форму заполняли дольше 3 секунд. Без
  капчи. Ловушка сработала — показываем «принято», ничего не отправляем.
- Согласие на обработку данных — чекбокс со ссылкой на `/privacy/`, обязателен.
- Обещание по московскому времени (`src/lib/callback.js`) — общая функция:
  `callbackWhen()` → «в течение 15 минут» / «завтра с 9:00» / «в понедельник
  с 9:00» (подзаголовок формы FAQ), `callbackPromise()` → «Перезвоним …»
  (формы заявки). Правило: Пн–Пт 9:00–17:44 → 15 минут; Пн–Чт с 17:45 и
  Вт–Пт до 8:59 → завтра с 9:00; Пт с 17:45, Сб, Вс, Пн до 8:59 →
  в понедельник с 9:00. Проверка: `?now=2026-09-26T20:00` (московское время).
- После отправки форма скрывается, показывается подтверждение с обещанием;
  при ошибке — сообщение, введённый текст остаётся. Без всплывающих окон.
- Телефон компании: +7 (8332) 77-57-70, `PHONE` в `src/data/calculator.js`.
  Маска и нормализация — `src/lib/phone.js`.

## Метрика

`track(goal, params)` в `src/lib/track.js`: `YM_COUNTER_ID = null` — без
счётчика функция молчит. Цели, которые уже вызываются:

| Цель | Когда |
| --- | --- |
| `benefits_cta` | кнопка «Собрать свою баню» в блоке «Что вы получаете» |
| `compare_cta` | кнопка «Узнать цену Подковы» в конце блока сравнения |
| `calc_season_change` | смена сезона в калькуляторе (`season`) |
| `calc_card_select` | выбор карточки (`card_id`) |
| `calc_submit` | успешная отправка формы калькулятора |
| `business_tab_open` | открыли вкладку «Для бизнеса» |
| `business_submit` | успешная отправка формы «Для бизнеса» |
| `process_cta_click` | кнопка в блоке «Как проходит заказ» |
| `reviews_show_more` | раскрыли отзывы на телефоне |
| `faq_open` | раскрыли вопрос (`question` — номер 1–18) |
| `faq_question_submit` | успешная отправка формы вопроса в FAQ |
| `visit_button` | кнопка в карточке блока показа (`type`: `showroom` / `production` / `video`) или ссылка из FAQ № 17 (`type`: `faq`) |
| `visit_submit` | успешная отправка формы записи на показ (`type`) |
| `works_photo_open` | открыли увеличение фото объекта (`name` — название объекта) |
| `works_calc_click` | «Рассчитать такую» в карточке объекта (`name`); дальше уходят обычные `calc_season_change` и `calc_card_select`, если сезон или карточка сменились |
| `final_submit` | успешная отправка финальной формы |
| `vk_click` | ссылка «ВКонтакте» в подвале |
| `map_view` | карта загрузилась и показана |
| `map_point_click` | клик по точке на карте (`name`) |
| `map_far_click` | клик по месту в строке «А ещё» (`name`) |
| `phone_click` | клик по любой ссылке `tel:` (глобальный слушатель в `main.jsx`, включая шапку и подвал) |

Цели отправки — только при успешной отправке. Проверить вызовы без счётчика
можно временной сборкой с подставленным `YM_COUNTER_ID` и заглушкой `window.ym`
(в репозиторий такую сборку не коммитить).

## Калькулятор

- Данные — только в `src/data/calculator.js`: сезоны, карточки (id, название,
  размер, планировка, фото, цена, состав, доставка), примечания, цитата,
  тексты «Для бизнеса», телефон. `MIN_PRICE` считается оттуда.
- Планировки (`LAYOUTS`) в одном масштабе: viewBox рассчитан на 6 м,
  длина прямоугольника пропорциональна длине бани. Без метражей комнат.
- Карточки — радиогруппа (стрелки, пробел, фокус, `aria-checked`).
- После выбора: пауза 250 мс, прокрутка к панели формы (верх на 96 px),
  рамка панели 1,2 с `accent`; на компьютере фокус в телефон, на телефоне нет.
  Если форма видна — только строка-итог. На телефоне, пока форма за экраном,
  снизу закреплена полоска с итогом и кнопкой.
- `Segmented`: размеры `md` / `sm` / `xs` (`xs` — три варианта в ряд на
  телефоне: текст 13 px, до 400 px — 12 px), `full` — на всю ширину, `scheme`
  `light` / `dark` / `accent` (`dark` — для карточек на `forest`, `accent` —
  выбранная половина `accent` с текстом `ink` на светлой карточке FAQ).

## Отзывы

- Данные только в `src/data/reviews.js`: `SOURCES` (Авито 5,0 · 40 оценок,
  Яндекс Карты 4,5 · 14 оценок, имена файлов иконок), `REVIEWS` (семь карточек
  по порядку), `BUSINESS_REVIEW`, `MOBILE_VISIBLE` = 3.
- Плашки рейтинга: квадратная иконка 64 px, рейтинг 28 px и `RatingStars`
  (инлайн-SVG, 5 звёзд по 20 px, заполнение считается из числа `rating`,
  4,5 даёт половину пятой звезды, `aria-label` «Рейтинг 4,5 из 5»).
- `ReviewCard`: метка-заголовок 20 px, круглая иконка площадки 40 px в правом
  верхнем углу, текст 16 px в пять строк (`line-clamp-5`) с «Читать полностью»
  / «Свернуть» (переполнение через ResizeObserver), внизу «Имя · дата».
  Подложка `surface-2`, скругление 6 px, поля 24 px, линия 3 px accent слева.
  Раскрытые отзывы хранит блок (`openIds`), чтобы не терять при смене раскладки.
- Иконки площадок — официальные файлы, WebP с прозрачностью, качество 85,
  каждая меньше 15 КБ, логотипы не рисовать. Круглые `review-*-{112,80,56}.webp`
  (в карточках показ 40 → файл 80), квадратные `rating-*-128.webp` (показ 64).
  Тень `drop-shadow(0 2px 8px rgba(26,21,18,0.18))`.

## Наши работы

- Данные только в `src/data/works.js`: `id`, `name` (для целей), `caption`
  (буквально из задания, без цен и сроков), `photos` (два кадра: `name`,
  `alt`), `calc` (`season` + `cardId` из `data/calculator.js`, `null` — без
  кнопки). Зониха → `warm-3`, Дороничи и Слободской → `year-45`,
  Кирово-Чепецк и Бабичи → `year-6`.
- Фото `obekt-N-<место>-glavnoe` и `-2`: 4:3, `-1280.webp` до 200 КБ и
  `-640.webp` до 80 КБ, качество 80, при перевесе до 75; если и на 75 не
  влезает — лёгкое шумоподавление перед сжатием (`blur(0.6)` в sharp, помогло
  трём летним кадрам с травой), ниже 70 не опускать. Без EXIF: это участки
  клиентов, координаты в файлах недопустимы.
- Увеличение (`works/Lightbox`): `role="dialog"`, фокус на «×» и обратно на
  фото, прокрутка страницы под ним выключена, второй кадр подгружается
  заранее через `new Image()`.

## Карта «Где стоят наши бани»

- Данные только в `src/data/objects.js`: `name`, `region`, `lat`, `lon`,
  `group` (`kirov` — одна точка на центре Кирова, подпись «Киров и пригороды —
  больше 15 бань»; `oblast` — 36 мест; `far` — Сыктывкар, Печора, Вухтым,
  Объячево, Нарьян-Мар). Красное и Краснодар пока не показываем.
- Координаты пишет `scripts/geocode.mjs` через HTTP Геокодер (тот же ключ):
  `VITE_YMAPS_KEY=… node scripts/geocode.mjs`. Скрипт переписывает массив и
  печатает спорные места (несколько вариантов, точность ниже населённого
  пункта, точка `oblast` вне границ 56,0–61,1 / 46,3–53,9). Запрос идёт
  с Referer сайта. Страница геокодер не вызывает.
- Ключ — только из `VITE_YMAPS_KEY` при сборке. Ограничение ключа в кабинете
  разработчика: HTTP Referer `volkov43-beep.github.io` и `banivyatki.ru`.
- Карта живёт в `components/works/MapBlock.jsx` внутри блока «Наши работы».
- API грузится через `lib/ymaps.js`, когда блок подходит к экрану
  (IntersectionObserver, запас 300 px), один раз за визит (тариф бесплатный,
  100 загрузок в сутки); до загрузки плашка `surface-2` того же размера.
  Без ключа, без сети или при ошибке карты нет вовсе: остаются заголовок,
  подзаголовок и список — без пустого прямоугольника. Причина — в консоли
  браузера, без значения ключа: «Карта: ключ не передан в сборку»,
  «Карта: скрипт Яндекса не загрузился …» (`code` `script` / `timeout`),
  «Карта: ошибка API — …» (`code` `api`, текст ошибки Яндекса). Модуль
  кнопок +/− необязателен: не загрузился — карта работает без него.
- Карта: начальный вид по границам точек `kirov` + `oblast` (`HOME_BOUNDS`,
  порядок API v3 — `[долгота, широта]`, границы `[[запад, север], [восток, юг]]`),
  приглушённый стиль через `customization` слоя `YMapDefaultSchemeLayer`
  (правила только по конкретным `tags`, границы `admin` без заливки, только
  линией), точки — DOM-маркеры
  `.bv-map-point` (accent 14 px, Киров 24 px с «15+»), подпись по клику
  `.bv-map-tip`. Без `scrollZoom` (колесо прокручивает страницу); на экранах
  с `pointer: coarse` без `drag` — одним пальцем прокручивается страница,
  двумя двигают карту. Из элементов управления только +/−; копирайт и логотип
  Яндекса остаются. Инициализация привязана к одноразовому флагу `shouldLoad`,
  а не к `status` (иначе смена статуса уничтожала карту).

## Частые вопросы

- Данные только в `src/data/faq.js`: `n`, `group` (на странице не показывается),
  `q`, `text` (абзацы через пустую строку, ссылки `[текст](#id)`), `list`,
  `after`. `plainAnswer()` даёт текст без разметки для JSON-LD.
- Сознательно не включены: печь (марка, мощность), порода дерева по
  комплектациям, расстояния до забора и дома, возврат аванса (нужно решение
  Ильи — тогда вопрос № 19), сравнение с бочкой (есть блок).
- В № 17 в конце ответа ссылка «Записаться на показ» на `#visit-form`:
  по клику `requestVisit('showroom', 'faq')` из `lib/visit.js` — плавная
  прокрутка к форме блока показа, в переключателе «Шоурум», цель
  `visit_button` с `type: "faq"`. Без JS ссылка работает как обычный якорь.

## Обработка картинок

- Только WebP в `public/photos/`, оригиналы (JPG/PNG с фотосессии и от
  дизайнера) в репозиторий не коммитить. В самих снимках ничего не менять:
  без ретуши, фильтров и дорисовки. Сжатие и обрезка под пропорции — да.
- Фото: качество 80. Ширины: карточки калькулятора 800 px, фото в блоках
  1120–1200 px, пары фото 600 px, первый экран 1920 / 1280 / мобильная 1080.
  Лимиты веса из заданий: одиночное фото до 150–180 КБ, парное до 90 КБ.
- Блок «Что внутри» (`vnutri-*`): те же правила, что у блока показа;
  горизонтальные 4:3, вертикальные 3:4 (`ratio="3/4"`). `vnutri-uteplenie`
  получен только в 684 px (WebP), поэтому есть одна ширина 640 —
  `widths={[640]}`; при появлении исходника добавить 1280.
- Блок показа (`shourum-*`, `proizvodstvo-*`, `video-zvonok`): 4:3, две ширины
  `-1280.webp` (до 200 КБ) и `-640.webp` (до 80 КБ) через `srcset`/`sizes`
  (`ResponsivePhoto`); качество 80, при перевесе до 75, не ниже; без
  EXIF. `proizvodstvo-3-dugi-1280` на 75 весит 218 КБ — принято.
- Чертежи: WebP с прозрачностью, качество 85, ширина 1440–1600 px, каждый
  меньше 200–250 КБ, без скругления углов; обрезка пустых полей с запасом 3 %,
  зоны под подписи не срезать; фон проверить по альфа-каналу.
- Иконки: WebP с прозрачностью, качество 85, двойная плотность, меньше 15 КБ.
- Конвертация через sharp (в проекте не установлен, ставить в отдельной
  папке): `sharp(src).resize({ width }).webp({ quality })`, круглая или
  скруглённая маска через `composite` с SVG и `blend: 'dest-in'`.
- `<img>`: `loading="lazy"`, явные `width` и `height`, скругление 6 px у фото.
- `PhotoSlot`: `src` относительно `public/`, `aspect`, `objectPosition`,
  `scheme="light"` на светлом фоне, `className` (`rounded-md`), `captionClass`;
  пустой `src` — полоса-заглушка «фото».
- Логотип: `logo-128.webp`, `logo-256.webp`, из него же favicon и apple-touch-icon.

## Проверка перед пушем

- `npm run build` без ошибок.
- Скриншоты на 1440, 1024, 768, 390 через Playwright с Chromium из
  `/opt/pw-browsers` (`executablePath: '/opt/pw-browsers/chromium'`).
  Playwright ставить в отдельной папке вне репозитория. Google Fonts в
  песочнице недоступны — подменять через `route`, поэтому на скриншотах
  системная гарнитура. Внешней сети (Яндекс и т. п.) в песочнице нет.
- Проверять по DOM и пикселям, не на глаз: тексты сверять с заданием,
  положение подписей — в единицах viewBox, цвета — `getComputedStyle`,
  контраст текста на фото — по реальным пикселям.
- Горизонтального скролла быть не должно. Ленивые картинки перед замером
  прокрутить в зону видимости.
- В отчёте отдельно называть, что проверить не удалось (нет сети, нет ключа).

## Известные проблемы и долги

- `muted` на светлом даёт 3,8:1 (на `surface-2` — 3,5:1) — AA для мелкого
  текста не проходит, принято.
- `accent` как цвет текста на светлом фоне: 2,5:1 на `surface`, 2,3:1 на
  `surface-2` — ниже нормы и для мелкого текста, и для крупного (нужно
  3:1). Это цены в карточках калькулятора, адреса в карточках показа,
  «Ничего» в «Как проходит заказ», номера шагов в ленте и все ссылки-кнопки
  (`link`) на светлом. Известно с момента появления, решение отдельным
  заданием.
- Карта: координаты в `objects.js` предварительные (геокодер не запускался),
  ключ добавлен в секреты и ограничен по Referer (`volkov43-beep.github.io`,
  `banivyatki.ru`, `www.banivyatki.ru`), но живой вид, стиль и жесты
  не проверены (в песочнице только заглушка API). Красное и Краснодар ждут
  уточнения места.
- FAQ, подтвердить до публикации: № 3 (про наценку за безнал не пишем — ок?),
  № 4 (до какого момента можно менять комплектацию), № 13 (вентзазор во всех
  круглогодичных; «пол у Подковы двойной проливной» без оговорки про
  комплектацию, а в блоке сравнения сноска — только круглогодичные).
- Переключатель «Шоурум / Производство / Видеозвонок» на экранах уже 400 px
  идёт текстом 12 px; на 360 px с запасной системной гарнитурой слово
  «Производство» на 2 px шире ячейки (с Onest помещается).
- Предзагрузка hero проверена только в Chromium: Firefox, ради которого
  ссылка стала одной и вставляется скриптом, в песочнице нет.
- Файлы `review-*-112.webp` и `review-*-56.webp` больше не используются
  (в карточках 80, в плашках квадратные 128); оставлены по условию задания.
- В главной схеме «пол 2000 мм» цветом accent (как в прежней схеме), хотя
  общее правило — muted-on-dark. На телефоне «высота над головой» 12 px.
- Выноски на чертеже «Дуга стен» проходят поверх серого силуэта — по заданию.
- Подпись «Лунный паз» под лупой на чертеже обвязки стоит впритык на 1024.
- Формы не отправляют заявки (console.log), счётчик Метрики не подключён.
- «Напишите в MAX» — способ связи в формах, ссылки на MAX нет (адреса
  в проекте нет).
- Год в копирайте подвала «2026» написан руками в `Footer.jsx`.
- Рамка второстепенной кнопки 1,5 px: Chromium при плотности 1 рисует её
  в 1 px, на плотных экранах 1,5.
- Фото утепления в «Что внутри» есть только в 640 px: на плотных экранах
  в колонке 552 px оно мягче остальных. Нужен исходник PNG крупнее.
- Подпись «крышка люка» (`accent`, 700) на фото слива: контраст со светлой
  стеной под ней 1,84:1 (в худшей точке 1,48:1). Места светлее в кадре нет,
  на тёмных досках `accent` почти сливается (≈1,1:1).

## Что дальше (очередь задач)

1. **Блок «Что внутри»** — в работе: следующее задание по нему ещё не
   поставлено. Известно: фото утепления нужно в 1280 px (ждём исходник),
   тексты тем живут в `SectionInside.jsx`.
2. **Иллюстрации и инфографика в блоке сравнения** — две колонки «в бочке /
   в Подкове» по каждому преимуществу. Сюда же чертёж проливного пола
   `diagram-pol.png` вместо `DrainFloorDiagram` (ждём ответа про слив).
   Материалы от дизайнера — PNG, конвертируем в WebP по правилам чертежей.
3. **Карта «Где стоят наши бани»** — код влит (PR #18), ключ в секретах и
   ограничен по Referer. Осталось: посмотреть карту вживую (если её нет —
   читать сообщение «Карта: …» в консоли), запустить `scripts/geocode.mjs`,
   проверить спорные точки, стиль, жесты, копирайт.
4. **Интеграция с Битрикс24** через PHP-обработчик `lead.php` на Beget:
   принимает `POST` с payload от `submitLead`, проверяет ловушку и обязательные
   поля, создаёт лид через входящий вебхук (`crm.lead.add`). Адрес вебхука —
   только на сервере, в чат его не присылать; `submitLead` меняется на
   `fetch(HANDLER_URL, …)`. Источник заявок в CRM — Яндекс.Директ,
   ответственный за лиды — РОП (руководитель отдела продаж): это поля лида,
   их согласовать при настройке.
5. **Счётчик Метрики**: `YM_COUNTER_ID` в `src/lib/track.js`, проверка всех
   целей из таблицы «Метрика» на живом сайте.
6. **Переезд на banivyatki.ru**: Referer ключа карт, base path сборки
   (`--base=/` вместо `/banivyatki-landing/`), workflow деплоя, `page_url`
   в заявках.
7. **Чек-лист перед запуском**: формы отправляют в Битрикс24, цели считаются,
   карта работает, политика доступна, FAQ подтверждён, скриншоты на
   1440 / 1024 / 768 / 390, нет горизонтального скролла, `git log -p -S`
   без ключей. **Директ не включаем до подключения Битрикса.**
