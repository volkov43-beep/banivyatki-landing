/**
 * Единственная точка отправки заявок.
 *
 * ВНИМАНИЕ: интеграция с Битрикс24 — следующее задание. Сейчас заявка
 * только пишется в console.log и считается отправленной.
 * Не запускать рекламу, пока эта функция не отправляет данные в CRM.
 */
export async function submitLead(payload) {
  console.log('[lead]', payload)
  return { ok: true }
}
